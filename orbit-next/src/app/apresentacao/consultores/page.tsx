import { PageContent } from './content';

export const metadata = {
  title: 'Apresentação — Consultores | Orbit Gestão',
  robots: { index: false, follow: false },
};

export default function Page() {
  return <PageContent />;
}
