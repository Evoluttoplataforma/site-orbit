import { PageContent } from './content';

export const metadata = {
  title: 'Segurança & IA — Orbit Gestão',
  description: 'Transparência total sobre quais IAs usamos, como protegemos seus dados e nossa conformidade com a LGPD.',
};

export default function Page() {
  return <PageContent />;
}
